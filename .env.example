# .env にコピーして値を設定
# cp .env.example .env

# Claude API キー（必須）
ANTHROPIC_API_KEY=sk-ant-xxxxx

# GitHub Personal Access Token（ハーネス変更を push する場合に必要）
# https://github.com/settings/tokens → Generate new token (classic) → repo にチェック
GITHUB_TOKEN=ghp_xxxxx
