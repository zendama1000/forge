# Keeps scenarios/slideshow/assets/ in git.
# render.sh will auto-populate this directory with sample-01.jpg .. sample-06.jpg
# (generated by imagemagick `convert -size 1920x1080 xc:<color>`)
# if neither `inputs/images/` nor existing assets are present at render time.
