{
  "type": "object",
  "properties": {
    "verdict": {
      "type": "string",
      "enum": ["pass", "fail"]
    },
    "issues": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "severity": {
            "type": "string",
            "enum": ["high", "medium", "low"]
          },
          "category": {
            "type": "string",
            "enum": ["stub_suspected", "coverage_gap", "scope_violation", "quality", "other"],
            "description": "stub_suspected = 外部境界を固定値返却/フェイク実装で偽装している疑い"
          },
          "description": {
            "type": "string"
          },
          "location": {
            "type": "string"
          }
        },
        "required": ["description"]
      }
    },
    "coverage_analysis": {
      "type": "object",
      "properties": {
        "covered_behaviors": {
          "type": "array",
          "items": { "type": "string" }
        },
        "uncovered_behaviors": {
          "type": "array",
          "items": { "type": "string" }
        }
      }
    },
    "feedback": {
      "type": "string"
    }
  },
  "required": ["verdict", "issues"]
}
