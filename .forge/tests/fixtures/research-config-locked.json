{
  "mode": "validate",
  "locked_decisions": [
    {
      "id": "LD-001",
      "decision": "実装は bash + jq のみで行い、Node.js ランタイムは使用しない",
      "reason": "ハーネスは Node 非依存で動作する必要がある",
      "command_policy": {
        "denied_commands": ["node"]
      }
    },
    {
      "id": "LD-002",
      "decision": "テストは決定的であり、外部 HTTP API には依存しない",
      "reason": "CI で再現可能な検証を保証する"
    }
  ],
  "open_questions": ["ログフォーマットの詳細"]
}
