{
  "id": "bad-patch-sample",
  "type": "image_slideshow",
  "version": "1.0.0",
  "description": "agent_prompt_patch がオブジェクト（文字列ではない）のフィクスチャ。バリデータは型エラーで失敗する想定。",
  "input_sources": [
    {
      "type": "image_dir",
      "path": "inputs/images"
    }
  ],
  "quality_gates": {
    "required_mechanical_gates": [
      {
        "id": "output_exists",
        "command": "test -f output.mp4"
      }
    ]
  },
  "agent_prompt_patch": {
    "system": "これはオブジェクトなので拒否される",
    "user": "文字列のみ許容のはず"
  }
}
